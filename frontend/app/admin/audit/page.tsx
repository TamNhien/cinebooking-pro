"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { api, dateTime } from "@/lib/api";
import { getAuth } from "@/lib/auth";
import type { AuditItem } from "@/lib/types";
export default function AuditPage(){const [items,setItems]=useState<AuditItem[]>([]);const [q,setQ]=useState("");const [error,setError]=useState("");useEffect(()=>{const a=getAuth();if(!a||a.role!=="ADMIN"){location.href="/login?next=/admin/audit";return;}api<AuditItem[]>("/admin/audit").then(setItems).catch(e=>setError(e.message));},[]);const filtered=useMemo(()=>items.filter(x=>`${x.actorEmail} ${x.action} ${x.entityType} ${x.entityId} ${x.details}`.toLowerCase().includes(q.toLowerCase())),[items,q]);return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="section-kicker">SECURITY & AUDIT</p><h1 className="text-3xl font-bold">Nhật ký hệ thống</h1><p className="mt-2 text-slate-400">Theo dõi đăng nhập, check-in và thao tác nghiệp vụ quan trọng.</p></div><Link className="btn btn-secondary" href="/admin">← Admin</Link></div><input className="input max-w-xl" value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm email, action, entity..."/>{error&&<div className="text-red-300">{error}</div>}<div className="overflow-x-auto rounded-2xl border border-slate-800 bg-slate-950/60"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-900 text-slate-400"><tr><th className="p-3">Thời gian</th><th>Actor</th><th>Action</th><th>Đối tượng</th><th>Chi tiết</th><th>IP</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id} className="border-t border-slate-800"><td className="p-3 text-slate-400">{dateTime(x.createdAt)}</td><td>{x.actorEmail||"system"}</td><td><span className="rounded-lg bg-slate-800 px-2 py-1 font-bold">{x.action}</span></td><td>{x.entityType||"—"} {x.entityId&&<span className="text-xs text-slate-500">#{x.entityId}</span>}</td><td className="max-w-md truncate">{x.details||"—"}</td><td className="text-slate-500">{x.ipAddress||"—"}</td></tr>)}</tbody></table></div></div>}
